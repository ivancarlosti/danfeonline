services:
  fiscalhub:
    image: ghcr.io/ivancarlosti/fiscalhub:latest
    container_name: fiscalhub_app
    restart: unless-stopped
    ports:
      - "${PORT:-8080}:8080"
    env_file:
      - .env
